$(document).ready(function() {
  $("#categories").replaceWith("<%= j(render(partial: 'layouts/categories')) %>");
  $("#application").replaceWith("<%= escape_javascript(render(partial: 'types/show_types', locals: { type: @type, listings: @listings })) %>");
});   