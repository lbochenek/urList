$(document).ready(function() {
  $("#categories").hide();
  // $("#userInfo").hide();
  // $("#createForm").hide();
  $(".listing").replaceWith("<%= escape_javascript(render(partial: 'listings/edit_form', locals: { listing: @listing })) %>");
});   