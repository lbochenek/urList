$(document).ready(function() {
  $(function() {
    // var post = $(this);
    return $(this).on("ajax:success", function(e, data, status, xhr) {
      alert("hello");
      // return $(this).remove();
//       return $("<%= escape_javascript(render(partial: 'listings/listing', locals: { listing: @listing })) %>").prepend("#list");
     });
   });
});   