<% provide(:title, "Edit listing") %>

<%= render 'shared/listing_form'%>

	<% if current_user?(@user) %>
      <%= link_to "Delete Account", @user, class: "delete", method: :delete,
	                                  data: { confirm: "Are you sure?" } %>
	<% end %>