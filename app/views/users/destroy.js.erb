// used http://edgeguides.rubyonrails.org/working_with_javascript_in_rails.html

$(document).ready(function() {
  $(function() {
    return $("a[data-remote]").on("ajax:success", function(e, data, status, xhr) {
      $("#categories").hide();
      $("#application").replaceWith("success");
     });
   });
}); 